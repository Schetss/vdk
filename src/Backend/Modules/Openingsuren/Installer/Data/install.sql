CREATE TABLE IF NOT EXISTS `openingsuren` (
 `id` int(11) NOT NULL auto_increment,
 `meta_id` int(11) NOT NULL,
 `category_id` int(11) NOT NULL,
 `language` varchar(5) NOT NULL,
 `naam` varchar(255),
 `maandagvoormiddag_open` time,
 `maandagvoormiddag_sluit` time,
 `maandagnamiddag_open` time,
 `maandagnamiddag_sluit` time,
 `dinsdagvoormiddag_open` time,
 `dinsdagvoormiddag_sluit` time,
 `dinsdagnamiddag_open` time,
 `dinsdagnamiddag_sluit` time,
 `woensdagvoormiddag_open` time,
 `woensdagvoormiddag_sluit` time,
 `woensdagnamiddag_open` time,
 `woensdagnamiddag_sluit` time,
 `donderdagvoormiddag_open` time,
 `donderdagvoormiddag_sluit` time,
 `donderdagnamiddag_open` time,
 `donderdagnamiddag_sluit` time,
 `vrijdagvoormiddag_open` time,
 `vrijdagvoormiddag_sluit` time,
 `vrijdagnamiddag_open` time,
 `vrijdagnamiddag_sluit` time,
 `zaterdagvoormiddag_open` time,
 `zaterdagvoormiddag_sluit` time,
 `zaterdagnamiddag_open` time,
 `zaterdagnamiddag_sluit` time,
 `zondagvoormiddag_open` time,
 `zondagvoormiddag_sluit` time,
 `zondagnamiddag_open` time,
 `zondagnamiddag_sluit` time,
 `wij_zijn_op_vakantie` ENUM('Y','N'),
 `maandag_open` ENUM('Y','N'),
 `dinsdag_open` ENUM('Y','N'),
 `woensdag_open` ENUM('Y','N'),
 `vrijdag_open` ENUM('Y','N'),
 `zondag_open` ENUM('Y','N'),
 `zaterdag_open` ENUM('Y','N'),
 `donderdag_open` ENUM('Y','N'),
 `sluitingsdagen` varchar(255),
 `created_on` datetime NOT NULL,
 `edited_on` datetime NOT NULL,
 `sequence` int(11) NOT NULL,
 PRIMARY KEY (`id`)
) ENGINE=MyISAM DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;

CREATE TABLE IF NOT EXISTS `openingsuren_categories` (
 `id` int(11) NOT NULL auto_increment,
 `meta_id` int(11) NOT NULL,
 `language` varchar(5) NOT NULL,
 `title` varchar(255) NOT NULL,
 `sequence` int(11) NOT NULL,
 `created_on` datetime NOT NULL,
 `edited_on` datetime NOT NULL,
 PRIMARY KEY (`id`)
) ENGINE=MyISAM DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;